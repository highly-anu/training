import type { FeasibilitySignal } from '@/lib/feasibility'

export function useFeasibility(): FeasibilitySignal[] {
  // Goals deprecated - feasibility now handled via philosophy expectations
  // TODO: Update to use philosophy-based feasibility
  return []
}
